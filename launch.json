{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/app.js", // Adjust the entry point as needed
      "env": {
        "VCAP_SERVICES": "${workspaceFolder}/store/default-env.json"
      }
    }
  ]
}
